# keep your holdings private — they live in a GitHub secret, not the public site
notifier/portfolio.json
notifier/*.local.json
# local notifier token file (never commit)
notifier/.env
# private holdings seed for the tracker (stays on your computer only)
portfolio-seed.js
# rich menu image asset
notifier/richmenu.png
